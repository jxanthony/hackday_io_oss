<!DOCTYPE html>
<html>
<head>
  <title>Hackday</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container-fluid">
	<div class="row-fluid">

		<div class="span8">
			<h1>SharePoint Hack Day!</h1>
			<p class="lead">Democratizing hack day judging. Freedom from director tyranny at last!</p>
		</div>

		<div class="span12">
			<div id="login" class="span4 offset8">
				<% if current_user %>
				    Signed in as <strong><%= current_user.name %></strong>!
				    <%= link_to "Sign out", signout_path, id: "sign_out" %>
				<% else %>
				    <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
				<% end %>
			</div>
		</div>
	</div>

	<div class="row-fluid">
		<div class="span8">
			<ul id="nav" class="inline-block">
				<li><a href="/">Top Rated</a></li>
				<li><a href="/hacks/favorites">My Favorites</a></li>
			</ul>
			<%= yield %>
		</div>
	</div>
</div>

</body>
</html>
